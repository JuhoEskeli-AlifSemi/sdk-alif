# Must be slightly larger than JPEG_CAPTURE_MAX_BYTES in main.c to cover
# k_heap block header and alignment overhead (typically 8-16 bytes).
# 280 KB data + 1 KB overhead = 287744
CONFIG_VIDEO_BUFFER_POOL_SZ_MAX=431616
CONFIG_VIDEO_BUFFER_POOL_NUM_MAX=1
